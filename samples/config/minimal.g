language g0

# Minimal explicit configuration fixture.
#
# This is intentionally small: it establishes the configuration namespace
# without implying a standard library, runtime, or target API.
conf.env = {}
